services:
  vapt-checklist:
    build: .
    container_name: vapt-checklist
    restart: unless-stopped
    ports:
      - "7331:7331"
    volumes:
      - ./data:/app/data
      - ./index.html:/app/index.html:ro
